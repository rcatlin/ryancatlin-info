var React = require('react');

var Footer = React.createClass({
    displayName: 'Footer',

    render: function() {
        return (
            <h1>
                {'Footer'}
            </h1>
        );
    }
});

module.exports = Footer;
